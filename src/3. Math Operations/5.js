// Decimal numbers

var ourDecimal = 5.7;
var myDecimal = 0.009;

// Multiplying decimal

var product = 2.0 * 2.5;
console.log(product);

// Divide decimal

var quotent = 2.0 / 2.5;
console.log(quotent);

// Finding a remainder (Modulus)

var remainder;
remainder = 11 % 3;
console.log(remainder);